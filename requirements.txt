# ============================================================
# NEXUS TRADER — Python Dependencies
# Tested: Python 3.11.x on Windows
#
# INSTALL ORDER:
#
#   Step 1 — Core dependencies:
#            pip install -r requirements.txt
#
#   Step 2 — PyTorch (choose ONE depending on your GPU):
#
#     CPU only (no GPU / unknown laptop):
#            pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
#     NVIDIA GPU with CUDA 12.4 (e.g. RTX 4070 on main machine):
#            pip install "torch>=2.6.0" torchvision --index-url https://download.pytorch.org/whl/cu124
#
#   Step 3 — FinBERT NLP (requires Step 2 first):
#            pip install transformers>=4.35.0
#
#   Step 4 — Download FinBERT model (~438 MB, one-time):
#            python -c "from transformers import pipeline; pipeline('text-classification', model='ProsusAI/finbert')"
#
# ============================================================

# --- GUI Framework ---
PySide6>=6.6.0

# --- Charts ---
pyqtgraph>=0.13.3

# --- Database / ORM ---
SQLAlchemy>=2.0.0

# --- Market Data / Exchange ---
ccxt>=4.2.0

# --- Data Processing ---
pandas>=2.1.0
numpy>=1.26.0

# --- Technical Indicators ---
# NOTE: pandas-ta is broken on PyPI for pandas 2.x.
# Use the 'ta' library instead — actively maintained, pandas 2.x compatible.
ta>=0.11.0

# --- Machine Learning / Statistics ---
scikit-learn>=1.3.0        # required by hmmlearn as a dependency
hmmlearn>=0.3.0            # HMM regime classifier
arch>=5.3.0                # MS-GARCH volatility forecaster
gymnasium>=1.0.0           # RL agent training environments

# --- Security ---
safetensors>=0.4.0         # Mitigates CVE-2025-32434 torch.load vulnerability

# --- Market Data (Macro / Sector Agents) ---
yfinance>=0.2.36           # DXY, US10Y yield, SPX for MacroAgent

# --- News / Data Feeds ---
feedparser>=6.0.0          # RSS/Atom news feed parsing

# --- NLP / Sentiment ---
vaderSentiment>=3.3.2
# torch installed separately above (Step 2) to choose CPU vs GPU build
# transformers installed separately above (Step 3) — requires torch first

# --- AI Strategy Generation ---
openai>=1.3.0
anthropic>=0.7.0
google-genai>=1.0.0

# --- Visualisation (Walk-Forward Validation Reports) ---
matplotlib>=3.7.0

# --- Voice Input / Text-to-Speech ---
SpeechRecognition>=3.10.0
pyttsx3>=2.90
# PyAudio for microphone access:
#   Windows: pip install pyaudio
#   If that fails on Windows: download the .whl from
#   https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio and install manually
pyaudio>=0.2.13

# --- News / Social Data ---
praw>=7.7.0                # Reddit API
newsapi-python>=0.2.7      # NewsAPI headlines

# --- Notifications (Trade Alerts) ---
twilio>=8.0.0              # WhatsApp + SMS alerts (optional — only needed if enabled)

# --- Encryption (API key vault) ---
cryptography>=41.0.0

# --- HTTP ---
requests>=2.31.0

# --- Configuration ---
pyyaml>=6.0

# --- Utilities ---
python-dateutil>=2.8.2
pytz>=2023.3
